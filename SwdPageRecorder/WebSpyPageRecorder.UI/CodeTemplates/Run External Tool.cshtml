@using WebSpyPageRecorder.UI;
@using WebSpyPageRecorder.UI.CodeGeneration;
@using WebSpyPageRecorder.WebDriver;

@{

    ExternalGenerator run = new ExternalGenerator();

    string jsonFile = "WebElementDefinitions.json";
    string commandLine = "cscript //Nologo sample_ParserWebElements.js";

    WebSpyPageObject pageObject = Model.PageObject;
    run.SaveToJSonFile(pageObject, jsonFile);

}
@*~~~ 
     RunExternalProcess(string fullCommandLine, bool waitForExit (optional; default=true)) 
~~~ *@
@Raw(@run.RunExternalProcess(commandLine))
